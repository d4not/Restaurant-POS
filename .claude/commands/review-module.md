Do a full review of the specified module. Don't change anything yet — just report.

1. Read all files in the module directory
2. Check compliance with CLAUDE.md rules
3. List any bugs, code smells, or convention violations
4. Check that all form fields are properly persisted end-to-end (UI → API → DB)
5. Verify Zod schemas match Prisma models
6. Check error handling completeness
7. Output a prioritized list of issues

Module: $ARGUMENTS
